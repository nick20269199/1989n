# 交易者守则

## 造轮子前先问

写任何代码/装任何包之前：
1. 系统自带吗？
2. 已有工具能做吗？
3. pip 一行能解决吗？
4. 项目里已经有人写了吗？

## 不允许的

- 静默吞错误 —— 数据错了比程序崩了更危险
- 信任外部数据 —— 财经API返回 0/None/乱码 必须先校验
- 硬编码密钥 —— 所有 Token/Key/Secret 走 .env
- 改配置不备份 —— 定时任务断了 = 数据丢了 = 决策盲了
