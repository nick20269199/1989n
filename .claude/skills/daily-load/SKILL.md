---
name: daily-load
description: 晨间认知加载 v3 — 一键注入认知上下文。Cron 08:07 触发。
---

# 晨间认知加载 v3

> 一键完成所有加载，无需手动步骤。

## 执行步骤

### Step 1: 运行加载脚本（自动化所有数据读取）

```bash
cd D:/1989n/stock_analysis && /d/Python314/python daily_load.py
```

此单行替代旧版 Step 1~6 的所有手动操作：
- 读取最新日摘要 → 周期阶段/比率警报/预期差/凝练规则/自检
- 规则库状态 → 活跃规则/昨日新增
- 比率基线 → 活跃警报 z-score
- 收件箱统计 → 未处理条目数

### Step 2: 注入上下文

将脚本输出的结构化信息作为全天默认视角注入当前会话：

```
=== 今日认知加载 v3 ===
周期阶段: [阶段] (置信度: X%)
策略提示: [策略]
下阶段风险: [风险]

昨日凝练规则(N条):
  - [条件] → [动作]

比率警报(N个):
  - [比率名]: z_score = X.X

碰撞方向: [方向]

预期差关注:
  - [维度]: 共识→偏差

规则库: N条活跃

昨日错误: [根因]
Ego Check: [挑战核心假设的证据]
===
```

### Step 3: 收件箱处理（需要判断，半自动）

检查 `daily_load.py` 输出的收件箱统计。如有未处理条目：

```bash
# 拉取远程更新
cd D:/1989n && git pull --rebase 2>&1

# 读 inbox/ 下各文件，逐条处理
# - ideas.md → 有价值的纳入分析/知识库
# - links.md → 有价值的内容提取
# - questions.md → 能答的答，需研究的加入 open_questions.json
# - images/ → 用 vision.py 分析
# 处理完标记 [processed]，提交推送
```

### Step 4: 蒸馏简报处理

检查 `stock_data/distillation_brief.md`：如有待蒸馏文件 → 判断产出落点，更新规则/技能/记忆。

## 约束

- 运行不超过 5 秒
- 如果 digest 不存在 → 跳过，只做周期判定
- 如果周期置信度 < 0.4 → 标注"信号不明确，以混沌模式运行"
- 周一额外加载周审计结果
