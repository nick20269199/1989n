# 幻方新量化
> Claude Code 为大脑 + Python 为肌肉的量化交易系统框架
> 2026-05-13 首次对话梳理

---

## 缘起

交易员问了一个问题：DeepSeek 的算法是量化发展出来的吗？

答案是：DeepSeek 母公司是幻方量化（High-Flyer），创始人梁文锋是幻方联合创始人。幻方 2021 年开始囤 GPU，大规模分布式计算能力从量化交易迁移到大模型训练。DeepSeek 的 MoE 架构和极致成本控制，本质上就是量化思维——每微秒都要抠，每一分钱都要算。

于是交易员说：我不需要全自动量化交易软件，我需要 **Claude Code 帮我判断市场，量化计算给我数据支撑**。

这就有了这套框架。

---

## 核心认知：不对称优势

| | 幻方量化 | 这套框架 |
|---|---|---|
| 硬件 | 万卡自建集群（萤火/Fire-Flyer） | 无，不需要 |
| 因子 | 自动因子工厂，上千个弱信号 | 精选 15-20 个有效因子，定期衰减检测 |
| 回测 | 分布式全市场并行 | 向量化单机回测（够用） |
| 信号合成 | XGBoost/LightGBM/神经网络 | Claude Code 多维度交叉验证 + 因子打分 |
| 风控 | 独立系统，不可绕过 | 独立风控模块（U1 铁律） |
| 模型更新 | 每日重训 | 每日复盘 + 周度因子更新 |
| 最大优势 | 算力碾压 | 判断力碾压 + 语义理解 |

**关键洞察**：幻方全是数值模型，没有语义理解能力。Claude Code 能读新闻、读财报、读大V观点、做多维度交叉验证——这是机构也没有的不对称优势。

---

## 三层架构

```
┌─────────────────────────────────────┐
│        决策层 (Claude Code)          │
│  策略判断 / 信号融合 / 风控检查      │
│  持仓映射 / 每日复盘                 │
├─────────────────────────────────────┤
│        计算层 (Python)               │
│  因子计算 / 回测引擎 / 信号生成      │
│  市场体制检测 / 仓位管理              │
├─────────────────────────────────────┤
│        数据层 (SQLite + Parquet)     │
│  日线缓存 / 资金流向 / 财务数据      │
│  板块行业 / 宏观 / 新闻情绪           │
└─────────────────────────────────────┘
```

### 数据层
- SQLite 做日线缓存，避免重复调用 MCP
- Parquet 列存历史数据，回测用
- 数据校验：规则明确写了"外部数据不可信"——0/None/乱码必须拦截

### 计算层
- 因子计算：pandas + numpy + ta-lib，向量化批量算
- 回测引擎：向量化回测，分钟级跑 5 年数据
- 市场体制检测：趋势/震荡/危机三种体制，参数自适应
- 信号生成：多信号加权融合

### 决策层（Claude Code 核心价值）
- 多维度交叉验证：技术面 + 资金面 + 基本面 + 情绪面
- 市场体制判断：今天什么市场环境？
- 仓位决策：凯利公式 + 硬止损
- 每日复盘：因子衰减检测、策略调整

---

## 四阶段路线图

### Phase 1 — 数据管道
建立本地数据缓存体系，确保所有数据源可靠、可校验、低延迟访问。

### Phase 2 — 计算引擎
搭建因子计算框架和向量化回测引擎，覆盖核心策略模板。

### Phase 3 — 决策层
设计 Claude Code 每日决策流程，从数据到判断的完整链路。

### Phase 4 — 复盘进化
因子衰减分析、策略参数自适应优化、每日认知压缩复盘。

---

## 为什么不用现成框架（Backtrader/vnPy）

1. 交易员不是程序员——vnPy 光配置就得一周，学习成本太高
2. 需要的是决策辅助，不是全自动化——现成框架预设"全自动"，而交易员更需要"Claude 分析 → 人做决定"
3. Claude Code 是指挥官——现成框架把 AI 降级成辅助工具，这套架构里 Claude Code 是大脑

---

## 设计原则

- **风控不可绕过**（U1 铁律）：止损/仓位限制在运行中不可被推理质疑
- **数据不可信任**：外部数据必须校验，数值异常直接拦截
- **算力只做算力的事**：Python 算因子、跑回测，Claude Code 做判断
- **反馈闭环**：每一笔交易结果 → 复盘 → 因子衰减检测 → 策略调整
- **轻量起步**：不需要分布式、不需要万卡、不需要 Kafka——单机 SQLite + pandas 足够起步

---

## 延展阅读

- 路径：本文件
- 参考规则：`rules/common/coding-style.md`（造轮子前先问）
- 参考规则：`rules/security/safety.md`（U1 风控铁律）
- 参考规则：`rules/agents/architecture.md`（U1-U4 架构约束）
- 参考规则：`rules/knowledge-routing.md`（知识路由表）
